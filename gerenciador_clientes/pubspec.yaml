name: gerenciador_clientes
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^latest_version # Para iniciar o Firebase
  cloud_firestore: ^latest_version # Para usar o Firestore

flutter:
  uses-material-design: true
